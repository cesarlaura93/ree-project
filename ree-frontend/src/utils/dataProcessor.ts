// // utils/dataProcessor.ts
// import {EnergyData  } from "@/types/energy";
// export const processEnergyData = (rawData: EnergyData[]): ProcessedData => {
//     return {
//       values: rawData.map(transformValue),
//       percentages: rawData.map(transformPercentage),
//       timeline: createTimeBins(rawData)
//     };
//   };
